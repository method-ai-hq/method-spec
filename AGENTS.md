# Project instructions

- Use plain English. Keep proposals, implemented behavior, and measured results distinct.
- This is an independent public repository. Do not copy private parent source, history, account data, or run records into it.
- Do not read or enumerate any sensitive directory.
- Read README.md, proposals/method-3.md, PLAN.md, and HACKATHON.md before implementation.
- Preserve the method/2 baseline. Changes to the next format belong in its proposal until implemented and checked.
- Keep the core format small. A new execution type needs a concrete use that existing types cannot serve well.
- Work on main unless the user requests a branch. Fetch before changes when a remote exists; fast-forward pull only when clean. Preserve unrelated work.
- Commit completed changes and push main. Run an existing deployment after a push if one is added. This repository currently has no deployment target.
- Do not start paid runs without an operator-set spending cap. Never request secrets in chat; use the owner's hidden-input helper if a secret is needed.
- Update the contribution record when implementation or measured results change.
